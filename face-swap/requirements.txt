# Core swap stack
insightface>=0.7.3
onnx>=1.15
# Choose ONE: onnxruntime (CPU) OR onnxruntime-gpu (CUDA). Default to CPU.
onnxruntime>=1.17
opencv-python>=4.8
numpy>=1.24,<2.0
Pillow>=10.0
tqdm>=4.66

# UI
gradio>=4.20

# Optional enhancer — comment out if you don't need it (large dep tree).
gfpgan>=1.3.8
